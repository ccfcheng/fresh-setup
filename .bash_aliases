# Reload bash profile
alias reload='. ~/.bash_profile'

# Aliases for file system
alias ..='cd ..'
alias la='ls -a'
alias ll='ls -al'

# Shortcuts
alias code='cd ~/Coding'
alias gitref='git fetch && git checkout develop && git pull && git remote prune origin && git branch -a'

